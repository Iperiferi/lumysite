

## Ta bort schema markup-texten från landningssidan

Raden "Du behöver inte veta vad 'schema markup' betyder — vi sköter tekniken åt dig automatiskt." i AI Discovery-sektionen på `src/pages/Index.tsx` tas bort.

### Ändring
**`src/pages/Index.tsx`** — Ta bort `<p>`-taggen med den texten (rad efter den avslutande `</div>` för datapunkts-gridet).

